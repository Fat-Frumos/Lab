package com.epam.esm.dao;

import com.epam.esm.entity.User;

/**
 * Data access interface for managing users.
 */
public interface UserDao extends Dao<User> {
    // This interface inherits the methods from the Dao<User> interface.
}
